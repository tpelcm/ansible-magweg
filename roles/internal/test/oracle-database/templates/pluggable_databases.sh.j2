#!/bin/bash
# {{ ansible_managed }}
{% include './templates/_environment.j2' %}

sqlplus "/as sysdba" << EOF
COLUMN PDB_NAME FORMAT A15
SELECT PDB_ID, '=='||PDB_NAME||'==' "PDB_NAME", STATUS FROM DBA_PDBS ORDER BY PDB_ID;
EOF