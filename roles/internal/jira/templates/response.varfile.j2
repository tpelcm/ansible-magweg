# {{ ansible_managed }}

app.jiraHome={{ jira_home_version_home }}
app.defaultInstallDir={{ jira_home_version_app }}
httpPort={{ jira_web_port }}
{% if jira_lcm['operation'] == 'install' %}
app.install.service$Boolean=false
executeLauncherAction$Boolean=false
installation.type=INSTALL
{% endif %}
{% if jira_lcm['operation'] == 'upgrade' %}
launch.application$Boolean=false
installation.type=UPGRADE
{% endif %}
launch.application$Boolean=false
sys.adminRights$Boolean=true
sys.installationDir={{ jira_home_version_app }}
sys.languageId=en
